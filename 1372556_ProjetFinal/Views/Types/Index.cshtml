@model IEnumerable<_1372556_ProjetFinal.Models.Types>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTypes">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTypes">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTypes">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
